# Phase 04 — shared local secrets / URLs (optional)
# Prefer projects/llm-inference-platform/.env for the capstone stack.

# Hugging Face token — only needed for gated models with vLLM
# HF_TOKEN=

# Optional remote inference endpoint if not using Compose
# INFERENCE_URL=http://localhost:8000
